name: Build and test
on: [push, pull_request]
env:
    CELL_DEBUG=0
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: rustup update
            - run: cargo build
            - run: cargo test